"use client";

/**
 * DevToolsShield - Giữ hệ thống an toàn mà không can thiệp vào tính năng phóng to / thu nhỏ (zoom in/out) của trình duyệt.
 */
export default function DevToolsShield() {
  return null;
}
